name: Deployment pipeline

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
    types: [opened, synchronize]

jobs:
  simple_deployment_pipeline:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: part11

    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Intentionally fail pipeline for Discord test
        if: ${{ github.event_name == 'push' && contains(github.event.head_commit.message, '#fail') }}
        run: exit 1

      - name: Install dependencies
        run: npm install
      - name: Check style
        run: npm run eslint
      - name: Run npm test
        run: npm run test
      - name: Run npm build
        run: npm run build
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
      - name: Run Playwright tests
        run: npm run test:e2e
      - name: Trigger deployment
        if: ${{ github.event_name == 'push' && !contains(join(github.event.commits.*.message, ''), '#skip') }}
        run: curl ${{ secrets.RENDER_DEPLOY_HOOK }}

  tag_release:
    needs: [simple_deployment_pipeline]
    if: ${{ github.event_name == 'push' && !contains(join(github.event.commits.*.message, ''), '#skip') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Bump version and push tag
        uses: anothrNick/github-tag-action@4ed44965e0db8dab2b466a16da04aec3cc312fd8
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEFAULT_BUMP: patch

  discord_noti_success:
    needs: [simple_deployment_pipeline, tag_release]
    if: ${{ github.event_name == 'push' }}
    runs-on: ubuntu-latest
    steps:
      - name: trigger success noti
        if: ${{ success() }}
        uses: sarisia/actions-status-discord@v1.16.0
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          content: "✅ **Deployment Successful!** A new version deployed to production"
          title: "Deployment Details"
          status: ${{ job.status }}
          nodetail: true
          description: |
            **Commit:** ${{ github.sha }}
            **Message:** ${{ github.event.head_commit.message }}
            **Author:** ${{ github.event.head_commit.author }}

  discord_noti_failure:
    needs: [simple_deployment_pipeline]
    if: ${{ always() && github.event_name == 'push' && needs.simple_deployment_pipeline.result == 'failure' }}
    runs-on: ubuntu-latest
    steps:
      - name: trigger failure noti
        if: ${{ failure() }}
        uses: sarisia/actions-status-discord@v1.16.0
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          content: "❌ **Build Failed!** The pipeline encountered an error"
          title: "Failure Details"
          status: ${{ job.status }}
          nodetail: true
          description: |
            **Repository:** ${{ github.action_repository }}
            **Branch:** ${{ github.ref_name }}
            **Commit:** ${{ github.sha }}
            **Message:** ${{ github.event.head_commit.message }}
            **Author:** ${{ github.event.head_commit.author }}
            **Workflow:** ${{ github.workflow }}
            **Job:** ${{ github.job }}

            [[View Commit]](${{ github.event.head_commit.url }})
            [[View Workflow Run]](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
